{
  "name": "agentic-skills",
  "owner": {
    "name": "LittleSeneca"
  },
  "plugins": [
    {
      "name": "aws-readonly",
      "source": "./plugins/aws-readonly",
      "description": "Safe AWS access for Claude — read-only commands run automatically, writes are handed back to you as paste-able CLI commands. Credentials come from a read-only key file mounted in your Cowork folder."
    },
    {
      "name": "x-lookup",
      "source": "./plugins/x-lookup",
      "description": "Read-only X (Twitter) lookups for Claude — profiles, posts, search, follower counts, and posts by id via the X API v2. Uses an app bearer token mounted in your Cowork folder. Never posts, deletes, likes, or follows."
    }
  ]
}
